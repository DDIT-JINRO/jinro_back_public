# front url
app.front_url=${PROD.SERVER.FRONT_URL}
app.back_url=${PROD.SERVER.BACK_URL}
app.file_url=${PROD.SERVER.FILE_URL}

# 운영환경 포트 (보통 80, 443 또는 다른 포트)
server.port=80

# 운영환경 로깅 설정 (ERROR, WARN 레벨만)
logging.level.kr.or.ddit=WARN
logging.level.org.springframework=WARN
logging.level.org.mybatis=WARN
logging.level.root=ERROR

# 운영환경 보안 헤더 설정
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-stacktrace=never
server.error.include-exception=false

# 운영환경 데이터베이스 커넥션 풀 최적화
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000